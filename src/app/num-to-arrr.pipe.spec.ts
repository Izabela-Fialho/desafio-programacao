import { NumToArrrPipe } from './num-to-arrr.pipe';

describe('NumToArrrPipe', () => {
  it('create an instance', () => {
    const pipe = new NumToArrrPipe();
    expect(pipe).toBeTruthy();
  });
});
